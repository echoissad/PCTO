#include <stdio.h>

int main()
{
    int x = 4;
    int y = 5;
    int a = x + y;
    int b = x - y;
    int c = x / y;
    int d = x * y;
    int e = x % y;
    printf("addizione %d,\nsottrazione %d,\ndivisione %d,\nmoltiplicazione %d,\nresto %d.\n", a, b, c, d, e);
    return(0);
}